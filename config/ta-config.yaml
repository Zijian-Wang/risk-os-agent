# Technical analysis config (extensible)
# Add RSI, MACD, etc. as needed

phase:
  enabled: true
  config: phase-config.yaml

# Future: rsi, macd, volume, etc.
# rsi:
#   enabled: false
#   period: 14
